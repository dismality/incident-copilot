# Incident Copilot API

The Python control plane for Incident Copilot. It receives authenticated
Alertmanager webhooks, deduplicates alert fingerprints, runs evidence-led
investigations, enforces approval policy, executes allowlisted simulator tools,
and independently verifies recovery. See the repository-level README for setup,
architecture, and demo instructions.
